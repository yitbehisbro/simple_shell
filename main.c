#include "main.h"

/**
 * main - entry point.
 * @argc: argument count.
 * @argv: argument vector.
 *
 * Return: status code
 */
int main(int __attribute__((unused)) argc, char __attribute__((unused)) **argv)
{
	hsh_loop();
	return (EXIT_SUCCESS);
}
